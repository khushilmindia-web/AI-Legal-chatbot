# Task Tracker

Use this file as the single source of truth for manual task entry and sequential execution.
## Rules

1. Add tasks in the order they should be executed.
2. Every new task must start with status `Pending`.
3. Only one task may be in `In Progress` at a time.
4. A task can start only when all previous tasks are `Completed`.
5. Update task status as it progresses: `Pending` -> `In Progress` -> `Completed` or `Blocked`.
6. Do not proceed to the next task unless explicitly instructed.
7. Do not skip or reorder tasks.
8. If a task cannot proceed, mark it `Blocked` with a short reason and stop.
9. Always prioritize pending tasks in `tasks.md` over newly assigned tasks.
10. Always read and update `tasks.md` before starting any newly assigned task.
11. If a task is blocked, attempt resolution for up to 3 minutes before marking it `Blocked`.
12. Retry a `Blocked` task up to 3 times only.
13. If still blocked after 3 attempts, leave it with a clear reason and do not retry unless conditions change.
14. Do not run any command longer than 3 minutes; if it exceeds or is expected to exceed this limit, stop it, log the reason, and continue with the next safe step or task.

## Status Legend

- `Pending`: queued and not started yet
- `In Progress`: currently being worked on
- `Completed`: finished
- `Blocked`: cannot continue until an issue is resolved

## Task List

| Order | Task | Status | Notes |
| --- | --- | --- | --- |
| 1 | Scaffold Indian Kanoon integration config and backend service without enabling it in live chat flow | Completed | Added config placeholders and `backend/app/services/indiankanoon_service.py` |
| 2 | Add RSA public-private key generation and request signing utilities for API integration | Completed | Added generator script, signing utility, env placeholders, and usage docs |
| 3 | Wire `INDIAKANOON_API_TOKEN` from `.env` into the chatbot backend and route user legal queries through the Indian Kanoon API to generate concise, relevant, low-token responses | Completed | Token alias loads from `.env`, Indian Kanoon retrieval is active when needed, and concise grounded source data feeds answer generation |
| 4 | Verify database connection and ensure stable connectivity with successful read/write operations | Completed | Verified active SQLite path, user lookup, session write/read, and cleanup on `data/lawyer_ai.db` |
| 5 | Verify SMTP setup and confirm successful email delivery using test email | Completed | Current Gmail-backed SMTP config sent a reset email successfully through the backend mailer |
| 6 | Validate "Forgot Password" button functionality in `index.html` including event binding and API trigger | Completed | Forgot-password UI is served from `Frontend/auth.html`; link, modal forms, event bindings, and API targets are present and live |
| 7 | Test complete forgot-password workflow (frontend -> backend -> SMTP -> email delivery) | Completed | Verified signup, reset request, reset link/token capture, password reset confirm, old-password rejection, new-password login, and reused-token rejection |
| 8 | Migrate the project frontend and backend to port `5000` and ensure all internal connections use only `127.0.0.1:5000` with no remaining dependency on any other project port | Completed | Active runtime, env defaults, startup scripts, README, and legacy fallback entrypoints now point to `127.0.0.1:5000`; live `/health`, `/frontend/auth.html`, and protected `/frontend/index.html` checks passed |
| 9 | Fix the port `5000` bind/startup issues, align all startup paths to `127.0.0.1:5000`, and verify health/frontend reachability | Completed | Added the 3-minute command timeout rule, cleaned stale repo background processes, updated root startup to `127.0.0.1:5000`, and verified `/health` plus `/frontend/auth.html` on the active `5000` instance |
| 10 | Modify chatbot to fetch responses ONLY from India Kanoon API using token auth and disable/comment any LLM-based or other answer generation | Completed | Active `ChatService` now builds chat answers only from India Kanoon `/search/` results, returns the exact no-result fallback, preserves auth/history/upload flows, and was verified with a direct FastAPI `TestClient` smoke harness because pytest remains blocked by the repo's existing Windows temp-permission issue |
| 11 | Debug India Kanoon-only chatbot flow to resolve infinite loading and ensure proper API response handling | Completed | Added bounded frontend request timeouts, backend India Kanoon response/status/body logging, safer exception handling, and guaranteed fallback JSON/message handling so `/chat` and `/chat/upload` now resolve with “No relevant legal data found on India Kanoon” instead of hanging when India Kanoon fails or returns empty data |
| 12 | Fix frontend busy-state crash caused by null DOM selector in `setBusy()` and ensure chat loader always clears | Completed | Restored the missing `statusText` node in both frontend entrypoints and made `Frontend/app.js` busy/error/event handling null-safe so the chat loader clears reliably on success, error, timeout, and fallback paths without crashing on missing selectors |
| 13 | Fix India Kanoon statute-query retrieval so basic lookups like `Section 420 IPC` do not get lost behind state-only filtering | Completed | Updated chat search to classify statute/IPC-style prompts as criminal, try normalized query variants plus broader doctypes before state-specific filters, added multi-attempt result deduping in `backend/app/services/indiankanoon_service.py`, and verified the new path with direct Python smoke checks because pytest cleanup is still blocked by the repo's Windows temp-permission issue |
| 14 | Restore intent-first routing for greetings and simple handled intents, and fix India Kanoon live-request failures caused by the broken proxy environment | Completed | Added a new intent-routing layer ahead of India Kanoon in the active v2 chat flow, restored the missing `data/Json/Intents.json` asset expected by the trained intent classes, and hardened India Kanoon requests to ignore the broken local `127.0.0.1:9` proxy env by default so valid legal queries can reach the API instead of falling straight to the fallback |
| 15 | Sync the active intent-routing layer with the updated `data/Json/Intents.json`, refresh intent model assets, and stop non-legal/API-topic prompts from falling into India Kanoon fallback | Completed | Updated the active intent service to read the current JSON patterns/responses instead of stale hardcoded shortcuts, retrained the intent model artifacts from the edited intents file, hardened training/runtime tokenization against missing local NLTK corpora, and added a non-legal technical-topic guard so API/helpdesk-style prompts no longer route into India Kanoon and trigger the legal-data fallback |
| 16 | Add `legal_help` / `procedural` intent routing for process-based legal questions and restrict India Kanoon to case-law / section / judgment queries with procedural fallback on API misses | Completed | Added new `legal_help` and `procedural` intents in `data/Json/Intents.json`, retrained the intent model, updated routing so process-based legal questions go to a general legal-help handler instead of India Kanoon, restricted India Kanoon usage to case-law / section / judgment style queries, and made empty or failed India Kanoon lookups fall back to procedural legal guidance before ever returning the old no-data response |
| 17 | Preserve chat conversation state across turns so legal-help follow-ups continue instead of resetting into greeting or India Kanoon fallback | Completed | Added session-backed `active_intent` / `awaiting_details` state in chat sessions, disabled greeting reuse after the conversation starts, treated substantive issue statements as legal-help instead of greeting, and verified with a direct FastAPI smoke harness because pytest cleanup is still blocked by the repo's Windows `.pytest_tmp` permission issue |
| 18 | Make `legal_help` follow-up replies use newly provided details to advance the conversation instead of repeating generic templates | Completed | Added structured procedural slots and staged legal-help guidance so fraud/UPI prompts now return immediate actionable advice and replies like `Ahmedabad city` advance the police-complaint flow instead of replaying the same generic template; verified with a direct FastAPI smoke harness because pytest cleanup is still blocked by the repo's Windows `.pytest_tmp` permission issue |
| 19 | Harden intent routing so legal issue prompts cannot be misclassified as greeting or welcome responses | Completed | Reordered intent precedence so fraud/UPI/scam/bank-debit and other substantive legal-help signals win before greeting, added stricter greeting acceptance rules, and verified with direct Python and FastAPI smoke checks that issue statements no longer receive welcome responses |
| 20 | Temporarily disable live intent routing and switch chat to a single Indian Kanoon token-retrieval plus grounded LLM-answer pipeline | Completed | Rewired live chat to use one Indian Kanoon-first RAG pipeline with query normalization, multi-attempt token API search, doc/docfragment enrichment, grounded context assembly, OpenAI answer generation, strong per-stage logs, and final no-data fallback only after retrieval attempts fail; verified with a direct FastAPI smoke harness because pytest cleanup is still blocked by the repo's Windows `.pytest_tmp` permission issue |
| 21 | Run final live-path validation for fallback behavior on core statute and cyber-fraud queries and fix any failing stage in runtime code | Completed | Live validation found the exact runtime bug: Indian Kanoon `/search`, `/doc`, `/docfragment`, and `/docmeta` were being called with `GET` and returning `405 Method Not Allowed`; updated the client to use the live API's `POST` shape, improved statute-source ranking and grounded fallback synthesis, and revalidated both `section 420 ipc punishment` and `I clicked a fake UPI link and money got debited` through retrieval, parsing, document fetch, context assembly, and final runtime response generation |
| 22 | Fix LLM answer-generation failures so grounded Indian Kanoon retrieval always yields a structured final answer with better diagnostics | Completed | Hardened `backend/app/services/openai_service.py` with richer request/response diagnostics, prompt-size and bad-request handling, safer response-text extraction, and stronger JSON parsing; updated `backend/app/services/chat_service.py` to trim oversized grounded context, normalize LLM payloads, and synthesize a structured grounded answer with sources, caution, and document guidance whenever the LLM call fails or returns an empty answer |
| 23 | Enforce strict five-section final answer formatting and clean noisy Indian Kanoon / LLM output before returning chat responses | Completed | Final grounded answers are now normalized into Summary, Legal Position, Practical Next Steps, Sources, Disclaimer; HTML/debug/error noise and duplicate source text are scrubbed in `backend/app/services/chat_service.py`, and the behavior is documented in `changelog.md` |
| 24 | Improve authority retrieval and ranking so final answers use only the strongest relevant legal source and exclude noisy retrieval artifacts | Completed | Tightened `backend/app/services/indiankanoon_service.py` ranking/filtering to skip noisy results, reward exact authority matches, and keep only the strongest authority set; updated `backend/app/services/chat_service.py` to restrict context/citations to primary authorities so final answers stay clean, focused, and free of raw fragment/debug noise |
| 25 | Reintroduce intelligent chat routing so procedural/practical legal queries bypass India Kanoon while statute and case-law lookups still use grounded RAG | Completed | Updated `backend/app/services/chat_service.py` to route FIR/fraud/consumer/notice/document-style queries directly into structured legal-help guidance, wired `backend/app/services/intent_service.py` back into non-case-law routing, preserved India Kanoon only for section/statute/judgment/citation lookups, and kept strict five-section formatting across both direct-guidance and RAG answers |
| 26 | Make India Kanoon the primary legal source again and use Groq only for grounded explanation or reasoning fallback when retrieval is empty/irrelevant | Completed | Added `backend/app/services/groq_service.py` plus Groq config/env/dependency wiring, switched the live chat model stage from OpenAI to Groq, routed legal queries through India Kanoon first, used Groq to explain grounded authorities when retrieval is relevant, and fell back to cautious Groq reasoning only when India Kanoon retrieval is empty or insufficiently relevant while preserving strict five-section output |
| 27 | Revert the recent Groq integration and restore the previous India Kanoon-only working pipeline and behavior | Completed | Rolled back the active Groq runtime/config/test changes, restored the prior India Kanoon plus OpenAI-backed stable chat path and debug surface, and neutralized leftover Groq-only files without changing earlier routing, formatting, or retrieval-cleanup improvements |
| 28 | Make direct legal-help responses issue-specific so different practical disputes no longer reuse the same generic template | Completed | Split direct legal-help handling in `backend/app/services/chat_service.py` so food contamination/product-defect and snatching/theft issues are classified separately and produce materially different structured guidance, then added regressions to keep those paths from collapsing back into the same template |
| 29 | Make legal-help responses more legally realistic and fact-sensitive across common real-world situations | Completed | Reworked the legal-help answer builder so Summary, Legal Position, and Practical Next Steps are composed from issue type, available facts, forum, and document trail instead of broad line templates; scenario guidance now names concrete facts like bank/platform and complaint location where available, and follow-up city details now advance the theft/police workflow into more specific next steps |
| 30 | Rebuild legal-help into a virtual-advocate interview flow that asks one question at a time, gathers case facts, and answers only after enough details are collected | Completed | Added a stateful virtual-advocate intake flow with per-issue question plans, stored collected facts plus current question key in conversation state, made legal-help chats ask one targeted question at a time before advising, and switched the final legal-help response into a short advocate-style paragraph with optional numbered next steps once enough facts are gathered |
| 31 | Make the virtual-advocate final answer give the core legal position and typical process path with relevant BNS/BNSS-style section grounding instead of only general advice | Completed | Updated the post-intake virtual-advocate answer generator so completed legal-help interviews now state the likely legal framing and procedure route for common criminal-process matters, add issue-specific BNS/BNSS references in both the answer text and metadata, and keep the practical next steps tied to the same legal basis |
| 32 | Make intake responses give immediate legal guidance and actionable steps before each follow-up question | Completed | Updated the virtual-advocate interview flow so each intake reply now gives a short immediate legal direction on the facts already available, includes early legal/process grounding where relevant, and only then asks the next targeted follow-up question |
| 33 | Reorder intake replies to lead with plain-language step-by-step guidance, then optional law references, then only the necessary follow-up question | Completed | Updated intake replies so first-turn and mid-intake legal-help responses now start with plain-language step-by-step actions, mention the likely legal basis only after that when useful, and then ask only the next necessary follow-up question while continuing to use earlier answers as context |
| 34 | Refine intake replies to highlight the most urgent action and ask non-repetitive follow-up questions that use already collected facts | Completed | Updated intake wording so each reply now highlights the most urgent immediate step, keeps simple legal/process context only where useful, and phrases the next question with known facts like bank/app or city so the interview does not repeat already-collected details |
| 35 | Make intake replies name the place to act clearly while keeping one focused follow-up question | Completed | Updated intake guidance so it explicitly names the action forum such as bank or app support, 1930, police station, food safety authority, seller, consumer forum, or senior police officer where relevant, while keeping the urgent step obvious and asking only one context-aware follow-up |
| 36 | Ensure all legal-intake issues fall back to action-first guidance instead of India Kanoon no-data responses, and fix incorrect intake wording like 'contact UPI' | Completed | Expanded practical-issue detection for food, consumer, theft, landlord, and similar intake matters; on empty retrieval the chat now reroutes likely intake issues into the virtual-advocate path instead of returning the no-data fallback; and cyber-fraud wording now uses concrete bank/app language instead of vague terms like `contact UPI` |
| 37 | Make legal-intake replies sound like a real lawyer with brief acknowledgment, 2-3 immediate steps, and a natural follow-up question | Completed | Replaced label-heavy intake wording with concise, human lawyer-style replies that begin with a brief acknowledgment, give a few immediate actions in plain prose, avoid headings and robotic prompts, and then ask one natural follow-up question before fuller legal steps are given |
| 38 | Limit legal-intake questioning to the minimum needed and skip follow-up questions when the initial facts are already sufficient | Completed | Reduced intake plans so practical matters usually need at most 0-2 questions, added a sufficiency check to move directly to guidance when the first message already provides the key facts, and taught the intake state extractor to recognize obvious evidence such as packet/bill/photos for food issues so unnecessary follow-up questions are skipped |
| 39 | Audit the current project architecture, runtime flow, conversation state handling, India Kanoon integration, and operational weak points before any new code changes | Completed | Reviewed the active FastAPI app, frontend auth/chat flow, chat orchestration, session persistence, India Kanoon token wiring, and test setup; confirmed the token is loaded from the legacy `.env` alias and used by the active retrieval client, identified the fixed `.pytest_tmp` Windows permission blocker, upload-context loss in live chat, duplicate legacy architecture/docs drift, and auth-token storage/cookie hardening gaps, and recorded the findings plus action plan in `changelog.md` |
| 40 | Fix repeated or canned legal-help replies so follow-up chat turns progress with newly provided facts instead of replaying generic prompts | Completed | Updated the active `ChatService` legal-help continuation path to extract new issue-specific facts on every follow-up, stop storing city-only theft replies as raw timing text, and refresh final guidance with later evidence such as IMEI/invoice details instead of repeating the previous answer; verified with a targeted FastAPI regression harness because pytest temp-path cleanup is still blocked in this workspace |
| 41 | Fix conversation-state handling so follow-up turns consistently advance the current issue workflow across chats | Completed | Extended follow-up progression beyond legal-help into India Kanoon grounded flow by merging prior issue context for awaiting-details turns, preserving intent/domain state across turns, and adding regression coverage that confirms follow-up answers change with newly provided constraints instead of reusing earlier text |
| 42 | Fix upload grounding so extracted upload content materially affects retrieval and final answers | Completed | Wired extracted upload text into the grounded pipeline as contextual documents, included them in citations/context assembly for answer generation, removed the old upload-not-used warning behavior, and verified upload-grounded responses through targeted chat/upload tests |
| 43 | Address remaining high-priority issues from the architecture audit after chat-flow, state, and upload fixes | Completed | Resolved the critical pytest reliability blocker by removing fixed basetemp dependence, adding Windows-safe temp handling in test fixtures, and stabilizing fallback/session behavior so the full suite now runs reliably (`54 passed`) |
| 44 | Harden production auth/session handling (cookie security + frontend token storage) identified in audit | Completed | Completed through the focused hardening batch: frontend now uses cookie-first auth flow without token persistence, and backend cookie security adapts for non-local HTTPS contexts |
| 45 | Clean legacy/runtime documentation drift identified in audit | Completed | Completed through the focused cleanup batch: runtime docs now align with active `backend/app/main.py` architecture and legacy paths are explicitly documented |
| 46 | Stabilize pytest temp/cache handling so repeated full-suite runs are reliable without workspace permission side effects | Completed | Switched pytest to run without cache-provider writes in this ACL-limited workspace, moved Windows temp handling to run-scoped writable roots, added per-test temp cleanup, and verified reliability with two consecutive full-suite passes (`54 passed` each run) |
| 47 | Strengthen upload grounding so uploaded facts are explicitly carried into grounded answers and citations | Completed | Updated grounded prompt/normalization so uploaded facts are explicitly reflected in practical guidance when omitted by LLM output, ensured upload citations are preserved in final source lists, and verified with focused upload regression tests |
| 48 | Consolidate retrieval and conversation-state logic into one active runtime path and remove duplicate/unused service branches | Completed | Removed unused grounded/state helper branches from active `ChatService` so one retrieval + state progression path remains authoritative, then reverified grounded follow-up and authority-selection regressions |
| 49 | Harden auth/session runtime behavior by removing frontend token persistence and enforcing secure cookies in non-local environments | Completed | Removed frontend token persistence/Authorization-header dependence in `Frontend/auth.js` and `Frontend/app.js` (cookie-first session flow), added adaptive secure-cookie logic in backend auth utils, and verified with focused auth cookie tests for debug-local vs forwarded-https behavior |
| 50 | Archive/remove dead legacy code paths and align runtime documentation to current architecture | Completed | Marked root `app.py` as legacy in-file, refreshed README/runtime summary to match the active `backend/app/main.py` stack, and added explicit legacy guidance in `docs/LEGACY_NOTES.md` |
| 51 | Add focused regression coverage for upload grounding, grounded follow-up continuity, and auth/session cookie behavior | Completed | Added focused regressions for upload-fact reflection and upload citations, grounded follow-up state persistence/pipeline continuity, and secure-cookie behavior under debug-local vs forwarded-https requests; verified focused suites plus full `pytest tests -q` (`58 passed`) |
| 52 | Show legal section references only when user asks for them or when full facts are available | Completed | Updated legal-help intake flow to suppress section references/citations during partial-fact follow-up unless the user explicitly asks for sections, while preserving section-grounded output after full facts are collected; added focused tests for both behaviors |
| 53 | Tighten legal query classification, retrieval confidence, output validation, and fact-sensitive escalation in the active chat path | Completed | Added legal query strategy classification, statute-first/case-second retrieval bias, irrelevant-case filtering with safe authority fallback, grounded-confidence fallback, filler stripping, fact-sensitive criminal-law gating, and final output validation; verified with targeted regressions plus full `pytest tests -q` (`63 passed`) |
| 54 | Add a first-layer legal domain classifier so queries are labeled and routed by domain before India Kanoon retrieval | Completed | Added a lightweight legal domain classifier for `criminal`, `civil`, `tax`, `property`, `corporate`, `consumer`, `document_review`, and `procedure`, wired it into the first step of the active chat flow before retrieval, persisted the label in chat metadata/state, and verified with focused classifier and chat-routing tests |
| 55 | Build a temporary modular legal document ingestion pipeline for training-time parsing, cleaning, metadata tagging, and chunking | Completed | Added an isolated top-level `training` package with reusable dataclass models plus parser, cleaner, metadata, chunking, and orchestration modules designed for later extraction into a standalone ingestion service; verified with focused pipeline tests for HTML/JSON parsing, legal metadata tagging, and chunk generation |
| 56 | Batch-process the external legal corpus from `D:\Learning\data` into embedding-ready training artifacts using the temporary ingestion pipeline | Completed | Added a file-backed batch ingester plus JSONL/manifest writer for PDFs, TXT, JSON, HTML, and Markdown inputs, ran it against `D:\Learning\data`, and produced cleaned document/chunk artifacts plus run metadata under `data\training_ingestion\learning_data_corpus` for later embedding/storage |
| 57 | Add embedding/vector export output for the prepared training corpus so cleaned chunks are ready for storage or indexing | Completed | Added a modular embedding export layer with a deterministic local fallback and vector JSONL output, regenerated the `D:\Learning\data` corpus artifacts to include `embeddings.jsonl` and `vectors.jsonl`, and verified the export format and counts with focused tests |
| 58 | Add internal-first hybrid legal retrieval with smart chunking, live India Kanoon fallback, and strict grounded-answer prompting | Completed | Added domain-aware chunking, internal corpus indexing, hybrid keyword-plus-semantic retrieval, parallel/secondary India Kanoon lookup when internal results are weak or outdated, merged ranked context assembly, and a strict prompt that answers only from retrieved sources with source attribution and uncertainty handling; verified with focused retrieval/chat regressions and the full suite |
| 59 | Remove duplicated follow-up questions from the legal-help answer body so the prompt appears only once | Completed | Updated the legal-help interview and guidance builders so the question is shown only in the dedicated follow-up field and not repeated in the main answer text; added a regression to ensure the answer body stays question-free while the follow-up remains available |
| 60 | Standardize the whole assistant response style to use clear, slightly robotic wording with brief empathy and explicit urgency cues | Completed | Updated shared legal-help intake/completed-guidance style helpers plus the grounded RAG prompt so issue responses consistently use brief empathy, direct action-first wording, and time-sensitive phrasing including 24-hour recovery language for fraud; added focused regressions and verified the runtime with in-process FastAPI checks across cyber-fraud, notice, food-safety, and statute-query paths |
| 61 | Stop repeating already-completed instructions and move completed legal-help guidance to the next useful step | Completed | Updated completed legal-help guidance to detect when the user has already reported, complained, filed, or reached the notice stage, acknowledge that progress, skip the repeated step, and surface the next action instead; added focused regressions and verified the runtime with in-process FastAPI checks for cyber-fraud and food-safety progress states |
| 62 | Make each new conversation fully independent and reduce repeated response structure across different direct-guidance queries | Completed | Explicitly reset state/history for fresh chats so new conversations never consult prior chat context, and varied completed direct-guidance headings by issue type (`Immediate next steps`, `Response plan`, `What to do next`, etc.) instead of always reusing the same closing structure; added focused regressions and verified the runtime with in-process FastAPI checks across fresh-chat and multi-issue scenarios |
| 63 | Add an independent delete button for each chat so users can remove one specific chat without clearing the entire chat history | Completed | Added a single-chat delete path in the session store and chat API, wired a dedicated Delete button into each history card in the active frontend, and verified that deleting one chat removes only that session while leaving the others intact and keeping Clear History for bulk deletion |
